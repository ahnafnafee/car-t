# Cohort report

Run `python simulator/run_sim.py --n 200 --seed 1001` from the repository root.
The current report and plots are written to `simulator/out/`, or to `--outdir`.

See [the evidence map](../docs/EVIDENCE.md) for endpoint definitions and comparison limits.
