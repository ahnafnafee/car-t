# Engine comparison

Run `python simulator/diagnostics/compare_engines.py --seeds 10` from the repository root.
The command compares a noise-free ODE patient with repeated micro trajectories using
the same parameters and disabled antigen-loss mutations. It reports the median and
range without declaring the engines equivalent or clinically validated.
