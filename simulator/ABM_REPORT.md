# Assay report

Run `python simulator/abm_assay.py --crossval` from the repository root.
The current report and plots are written to `simulator/out/`, or to `--outdir`.

The assay-to-ODE rate comparison uses shared assumptions and is not independent
experimental validation. See [the evidence map](../docs/EVIDENCE.md).
